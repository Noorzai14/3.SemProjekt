namespace BarberAkji.Models.Enums;

public enum ServiceType // Enum til at definere forskellige typer af services med varighed i minutter
{
    Haircut = 30,      // Klipning tager 30 minutter
    BeardTrim = 15,    // Skægtrimning tager 15 minutter
    HairAndBeard = 45  // Klip + skæg tager 45 minutter
}

